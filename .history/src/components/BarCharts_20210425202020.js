import axios from 'axios';
import React, {useEffect, useState} from 'react';

const BarCharts = ({data, labels}) => {
  const [lablesVal, setLablesVal] = useState([]);

  return <div></div>;
};

export default BarCharts;
